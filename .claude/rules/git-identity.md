# Git identity — this repo only

Every commit in `invfin` uses `himy2u` / `him_y2u@yahoo.com` — never `hpalldata`. This is set
directly on this repo's local git config (not relying on the global `includeIf`, which is broken —
see `.claude/STATE.md`). Check `git config user.email` before the first commit of any session.
